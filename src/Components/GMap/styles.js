import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    map:{
        width: '100%',
        minWidth: 300,
        height: '100%',
        minHeight: 300,
        maxHeight: 600,
        border: 0
    }
});
export default useStyles;